pytest>=8
pdfplumber>=0.11
requests>=2.31
# SVG→PNG for the OG share cards. A prebuilt binary wheel (manylinux/macOS), so
# CI can re-render og-card.png from fresh data on every deploy.
resvg_py>=0.3.2
